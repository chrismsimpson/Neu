// Makes sure that the return type of `moop` is correctly identified before 
// `Class.foop` is typechecked.

func moop() => true

struct Class {
    func foop(this) {
        if moop() {
        }
    }
}

func main() {
    printLine("OK")
}
