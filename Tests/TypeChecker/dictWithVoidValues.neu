/// Expect:
/// - error: "cannot create a dictionary with values of type void\n"

enum Bar {
    value
}

func main() {
    let foo = Bar.value()
    [1: when foo {
        else => {
        }
    }]
}
