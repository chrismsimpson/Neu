class Person {
    name: String
    age: Int64

    func generate(name: String, age: Int64) -> Person {
        return Person(name: name, age: age + 1000);
    }
}

func main() {
    let p = Person.generate(name: "Jane", age: 100)

    printLine(p.name)
    printLine(p.age)
}
